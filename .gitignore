__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# рантайм-состояние сессий (не коммитим): журналы сделок + боевые account_id.
# Шаблон на ВСЕ движки — перечисление по номерам пропустило st8/st9 (14.08),
# они висели untracked и ушли бы в репозиторий первым же `git add -A`.
session_state_*.json

# токен T-Bank (секрет, НИКОГДА не коммитить)
.tbank_token
app/st4/.tbank_token
.tbank_account_tokens.json
app/st4/.tbank_account_tokens.json

# токен Telegram-бота (секрет, НИКОГДА не коммитить)
.tg_bot_token
app/st5/.tg_bot_token

# рантайм-артефакты (история бэктестов, скан-отчёты)
out/

# деплой-конфиги с внутренними адресами не публикуем при необходимости
# (здесь оставляем infra/ в репозитории — без секретов)

# служебные файлы Claude
.claude/
